# TriageX
TriageX — An intelligent support ticket routing engine that auto-classifies tickets into Billing, Technical, or Legal using ML (TF-IDF + Naive Bayes), detects urgency with NLP, and routes to the best available agent via skill-vector matching. Built with Django REST Framework + React.
